{
  "name": "core",
  "private": true,
  "scripts": {
    "compile": "tsc -b -w -i",
    "build": "npx nx run-many build",
    "eslint": "eslint src/**/**.{ts,tsx}",
    "eslint:fix": "eslint src/**/**.{ts,tsx} --fix"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "nx": "19.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "supabase": "^1.192.5",
    "typescript": "^5.6.2"
  }
}
